import UserDetail from "./components/UserDetail";

function User() {
    return (
        <div 
        className="flex flex-col h-screen w-full" // 전체 높이와 너비를 설정
    >
                < UserDetail />
            </div>
        );
}

export default User;